[pytest]
# Mark database tests for easy filtering
markers =
    database: tests that require database connection (skip with -m "not database")

# Test discovery
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = -v --tb=short

# Console output
console_output_style = progress
