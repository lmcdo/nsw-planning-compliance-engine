# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# QA railguards (ephemeral per-commit)
.qa_report.json

# Ad-hoc scratch DB query scripts (may contain connection strings) — never commit
tmp_db_query.py
tmp_*.py
scratch_*.py

# Python virtual environments
venv/
venv_linux/
__pycache__/
.ruff_cache/
.pytest_cache/

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
.next/
out/
*/.next/
frontend-nextjs/.next/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Temporary processing files
temp_extraction_*/
temp_processing/
logs/

# geotessera tile cache (large .npy files, downloaded on demand)
global_0.1_degree_representation/
global_0.1_degree_tiff_all/

# Database files (recreated by setup_database.py)
*.db
*.sqlite3
*.db-journal
nsw_planning*.db
regulatory_database.db

# Large processing outputs (regenerated by scripts)
output/
pipeline_output/
monitored_pipeline_output/
langextract_verified_output/
autoschema_database_output/
images/
*.json.bak

# Flood study shapefiles and raw spatial data (large binaries, not for VCS)
data/flood_studies/
data/spatial_raw/
# Downloaded spatial payloads — 1.9 GB as at 2026-08-11, same class as the two
# lines above. Re-fetchable, so the RECIPE is kept and the payload is not:
# coastal_inundation is 16 shapefile sets + their zips; flood_calibration is 47
# Copernicus EMS archives whose download URLs are recorded beside them.
# Written as `dir/*` not `dir/`, because git cannot re-include a file whose
# PARENT DIRECTORY is excluded — with `dir/` every `!` below would be inert.
data/coastal_inundation/*
!data/coastal_inundation/readme.txt
data/flood_calibration/*
!data/flood_calibration/copernicus_manifest.json
!data/flood_calibration/copernicus/
data/flood_calibration/copernicus/*
!data/flood_calibration/copernicus/*.urls

# Saved HTTP error page from a failed council scrape (an "Access Denied" body,
# not a document). Kept out rather than deleted in case the failure is evidence.
/kg_dcp_page.html

# Per-session tool output, like .qa_report.json above: a hook marker and a
# cross-review result. Both are regenerated and neither describes the codebase.
.claude/.prior-art-surfaces.json
.claude/sol-shadow-review.json
# Same class, missed when the others were listed: the marker written by
# /pre-impl that gates Edit/Write to scripts/, migrations/ and services/.
# Per-session and regenerated; committing it would hand the next branch a
# stale pass for an investigation it never ran.
.claude/.pre-impl-done.json

# One rollback backup exceeds the 2 MB pre-commit gate (8.3 MB): the predicted
# December-DST shadow values behind the #883/#885 regeneration. Kept ON DISK as
# evidence — memory records that these backups must not be deleted — but out of
# git, because the gate is right that an 8 MB JSON does not belong in history.
# Every other backup in that directory is under 600 KB and IS committed.
data/db_rollback_backups/shadow_december_dst_predicted_2026-08-07.json

# Generated conveyancing PDF reports (outputs, not source)
reports/conveyancing/*.pdf

# One-off diagnostic scripts (not part of the codebase)
scripts/_check_*.py

# Test trail files
test-trail/

# SAR validation outputs and local credentials (large GeoTIFFs, not for VCS)
validation_output/
scripts/drawdown_creds.txt
scripts/drawdown_run.bat

# Frontend Next.js specific
frontend-nextjs/node_modules/
frontend-nextjs/.next/
frontend-nextjs/.env.local
nsw-assessment/

# Build and cache files
*.pack.gz
*.trace
dev_server.log
migration.log
*.log

# Temporary markers and state files
migration_markers/
prp_checkpoints/*.marker
prp_checkpoints/*.log
migration_state.json
resume_config*.json

# Large executables and archives
*.exe
*.gz
*.zip

# Extracted/processed content (regenerated)
lightrag/
autoschema_multimodal_*.json
*_verified_output/
*_extraction_*.json

# Python cache (additional)
*.pyc
*.pyo
.ruff_cache/

# Backup and migration directories
migratePRPs/
PRPs/migratePRPs/
backup/
**/backup/
results/

# Long filename directories (Windows issue)
prp_k9_complete_dcp_extraction/
prp_k9_complete_lep_extraction/

# Test and verification outputs
verify_*.json
*_results.json
test_*.json

# ...but NEVER the per-branch QA reports. The slug is the branch name with
# slashes as "__", so a branch called test/x or verify/x produces
# .qa/reports/test__x.json, which the three patterns above silently swallow.
# An ignored QA report is not a missing file, it is a GATE THAT STOPS
# ENFORCING: CI treats an absent report as a warning and exits 0. That is
# exactly how #398 went unnoticed for ten weeks. Found again 2026-08-12 on
# branch test/feedback-route-validation.
!.qa/reports/*.json

# Analysis and diagnostic scripts (keep locally, don't commit)
analyze_*.py
check_*.py
fix_*.py
search_*.py
find_*.py
assess_*.py
audit_*.py
diagnose_*.py

# Session documentation (commit manually if needed)
*_COMPLETE.md
*_ANALYSIS.md
*_FINDINGS.md
*_STATUS.md
*_RESULTS.md

# ===========================================
# Working files - NOT for version control
# ===========================================

# BROAD: Ignore ALL root-level markdown except key docs
/*.md
!README.md
!CLAUDE.md
!PLANNING.md
!DEPLOYMENT.md
!TASK.md
!CHANGELOG.md
!screencast-*.md
!SETUP.md
!SECURITY.md
!SCREENCAST_API_CHECKLIST.md

# BROAD: Ignore ALL root-level Python scripts except core
/*.py
!setup.py
!test_workflow_quality.py

# BROAD: Ignore ALL root-level JSON except package configs
/*.json
!package.json
!tsconfig.json
!mutation-baselines.json
!dependency-skip-baseline.json

# Database backups (large binary files)
backups/
*.backup

# Extraction working directories
extraction_logs/
extraction_outputs/
extraction_temp/
enrichment/
# Whitelist enrichment config and extractor files (committed as part of core pipeline)
!enrichment/__init__.py
!enrichment/pipeline.py
!enrichment/precompute_amenity.py
!enrichment/config/
!enrichment/config/__init__.py
!enrichment/config/ashfield_config.py
!enrichment/config/leichhardt_config.py
!enrichment/config/marrickville_config.py
!enrichment/config/waverley_config.py
!enrichment/config/woollahra_config.py
!enrichment/config/city_of_sydney_config.py
!enrichment/config/ku_ring_gai_config.py
!enrichment/config/structural_categories.py
!enrichment/config/zone_taxonomy.py
!enrichment/extractors/
!enrichment/extractors/__init__.py
!enrichment/extractors/layer_topic_tagger.py
!enrichment/extractors/site_condition_tagger.py
!enrichment/extractors/type_classifier.py
!enrichment/extractors/numeric_extractor.py
!enrichment/extractors/actionable_classifier.py
!enrichment/extractors/applicability_tagger.py
!enrichment/extractors/marker_extractor.py
!enrichment/extractors/gemini_actionability_classifier.py
!enrichment/tests/
!enrichment/tests/__init__.py
# tests/enrichment/ is the pytest test suite (separate from enrichment/tests/).
# Must be explicitly un-ignored because the 'enrichment/' rule above matches at any depth.
!tests/enrichment/
!enrichment/tests/test_numeric_extractor.py
extractors/

# Temporary/working Python scripts
add_*.py
apply_*.py
backup_*.py
batch_*.py
calculate_*.py
categorize_*.py
clean_*.py
cleanup_*.py
compare_*.py
complete_*.py
comprehensive_*.py
configure_*.py
consolidate_*.py
copy_*.py
correct_*.py
count_*.py
create_*.py
debug_*.py
deduplicate_*.py
deep_*.py
delete_*.py
detect_*.py
determine_*.py
display_*.py
download_*.py
drop_*.py
enrich_*.py
examine_*.py
execute_*.py
explain_*.py
explore_*.py
export_*.py
extract_*.py
final_*.py
force_*.py
import_*.py
map_*.py
migrate_*.py

# Working markdown documentation (not core docs)
*_IMPLEMENTATION_PLAN.md
*_STRATEGY.md
*_GUIDE.md
*_SUMMARY.md
*_PLAN.md
*_FIX.md
*_ISSUE.md
*_DEBUG.md
*_AUDIT.md
*_REPORT.md
*_DIAGNOSIS.md
*_QUICKSTART.md
*_CHECKLIST.md
*_REFERENCE.md
*_EXPLAINED.md
*_REALITY_CHECK.md

# Temporary SQL files
*.sql
!migrations/*.sql
# Frontend migrations are source-controlled schema history as well.  The global
# rule above previously hid these files even though their README and project
# documentation instruct operators to run them.
!frontend-nextjs/migrations/*.sql

# The same "Temporary/working Python scripts" globs below (check_*.py,
# create_*.py, etc.) apply repo-wide and were also hiding root-level migration
# scripts, e.g. migrations/create_housing_sepp_standards.py — a real migration,
# not scratch. Found 2026-08-23 alongside the scripts/ gap below.
!migrations/*.py

# Working JSON files (not config)
*_extracted.json
*_provisions.json
*_to_import.json
*_test.json
*_backup*.json
*_mapping*.json
correct_*.json

# PowerShell scripts
*.ps1

# Temp/junk files
0
1
0.75
+
bullshit
continue
feature
fix
version
nul

# QGIS project files
*.qgz
*.qgs

# Misc working files
*.txt
!requirements.txt
!test-trail/*.txt
!frontend-nextjs/public/llms.txt
corrupted_*.txt
filter_*.txt
dcp_*.txt

# Claude local settings (keep .claude/prp and core docs)
.claude/settings.local.json
.claude/mcp.json
.claude/instructions.md
.claude/DQ*.md
.claude/SYSTEMATIC*.md
.claude/worktrees/

# Obsidian vault config (local only)
.claude/.obsidian/

# GitHub workflows (if not ready)
# .github/

# Environment backups
.env*.backup

# Local preservation bundles created during repository audits
.audit-preservation-*.bundle

# Local public-release export; reviewed separately before publication
/.public-release/

# Root-level junk files
version

# Frontend working files
frontend-nextjs/.env.production
frontend-nextjs/.env.vercel*
frontend-nextjs/check_*.js
frontend-nextjs/map_*.js
frontend-nextjs/debug_*.js
frontend-nextjs/appapicomplianceprecinct-requirementsroute.ts
frontend-nextjs/libprovision-title-utils.ts

# Root-level JS debug files
debug_*.js

# PRPs working directory
PRPs/

# Docs directory — DENY-LIST, not whitelist. Inverted 2026-08-20.
#
# This was `docs/*` plus a growing list of negations, which meant every NEW
# subdirectory was hidden by default, with no error and no git status entry.
# It cost two documents: PR #982's student brief (recovered) and PR #979's
# docs/pitch/style.html (gone from disk as well as git). Two other folders,
# docs/qa/ and docs/servicing/, had already been hand-patched after their own
# incidents — the trap was being fixed one directory at a time.
#
# The stated intent was always "ignore the large binaries, keep the writing".
# Measured 2026-08-20, `docs/*` was hiding 602 markdown files and 7 PDFs, which
# is the opposite. So: name the heavy and generated directories, and let
# documentation through by default.
#
# Adding a genuinely large directory here is now a deliberate line, and the
# 2MB-per-file pre-commit gate catches individual big files loudly. Bulk that
# slips past both is the residual risk, accepted knowingly.
docs/adg/
docs/history/
docs/mutation-testing/
docs/gephi/
docs/gipa-requests/
docs/*.pdf
docs/**/*.pdf
# Generated dependency graphs — dep-graph.svg is 1.2MB, under the 2MB per-file
# gate, so nothing else would stop it being committed on a stray `git add -A`.
docs/dep-graph*.svg

# PDF page images (stored in cloud, not git)
frontend-nextjs/public/pdf-pages/
**/pdf-pages/

# Frontend gitignore (already have root one)
frontend-nextjs/.gitignore

# Test files
test_*.js
test_*.ts
test_*.html
run_*.js
get_*.js

# Batch files
*.bat

# PDF.js worker files (large)
frontend-nextjs/public/pdf.worker.min.mjs
frontend-nextjs/public/pdfjs/

# Frontend misc
# vercel.json needs to be in git for region config

# Scripts directory (working scripts) - EXCEPT the explicitly named ones below.
# MUST be `scripts/*`, not `scripts/`: git cannot re-include a file whose PARENT
# DIRECTORY is excluded, so with `scripts/` every `!scripts/...` line below was
# inert and new files were silently untracked. The already-tracked ones survived
# only because gitignore does not apply to files already in the index.
scripts/*
# 2026-08-16: the allowlist below lost. 797 untracked .py files had accumulated
# in this directory, including the three the /classify-lga workflow tells you to
# run — populate_provision_types.py, backfill_structural_categories.py and
# backfill_heritage_hca.py — none of which existed on any clone but one laptop.
# A named allowlist only holds if every author remembers to extend it, and over
# 300 tracked files it stopped being remembered.
#
# So the rule is inverted: CODE IS TRACKED, and data, binaries and generated
# artefacts stay out. Nothing here needs adding when a new script is written,
# which is the only version of this that survives contact with a working week.
# The individually named lines further down are now redundant; they are left
# because each records why that file mattered.
!scripts/*/
!scripts/*.py
!scripts/*.sql
!scripts/*.sh
!scripts/*.mjs
!scripts/*.js
!scripts/*.bat
# The four lines above only reach ONE level deep — `*` does not cross `/` in
# gitignore, so scripts/definitions/, scripts/diagnostics/, scripts/fixes/,
# scripts/migrations/, scripts/procedural/, scripts/verification/ and
# scripts/sql/ were still being emptied by the basename globs in the
# "Temporary/working Python scripts" and "Temporary SQL files" sections above
# (check_*.py, fix_*.py, create_*.py, debug_*.py, *.sql, ...), which match at
# any depth. Same bug class as the fix above this comment block, one directory
# level deeper. Found 2026-08-23: scripts/definitions/import_definitions.py,
# scripts/diagnostics/check_roof_provision_text.py,
# scripts/procedural/import_procedural.py,
# scripts/verification/check_heritage_duplication.py and others were
# untracked and would not exist on a fresh clone.
!scripts/**/*.py
!scripts/**/*.sql
!scripts/**/*.sh
!scripts/**/*.mjs
!scripts/**/*.js
!scripts/**/*.bat
# Generated, and bulk source documents (three PDFs here total ~50 MB).
scripts/**/__pycache__/
scripts/**/*.pyc
scripts/**/*.pdf
!scripts/pr_overlap_guard.py
!scripts/r2_pdf_uploader.py
# Cross-reference resolution pre-task artifacts (permanent QA artifacts)
!scripts/xref_pretask_setup.py
!scripts/xref_detection_audit.py
# Precinct boundary health check (run before/after every boundary import)
!scripts/verify_precinct_boundaries.py
# Granny-flat review-state measurement — the read-only query behind the badge
# change (which states are DISTINGUISHABLE in stored rows, and the before/after
# for every existing report). Committed so the claims can cite a command.
!scripts/measure_granny_confidence_states.py
!scripts/measure_climate_reference_availability.py
# Read-only census behind the flood three-state change: how many stored
# reports served a 'not in a flood zone' that was never established.
!scripts/measure_flood_zone_unassessed.py
# Falsifiability plant-restore harness + the doc-claim checker. Both are
# imported by tests and invoked from .githooks/pre-push, so an untracked copy
# breaks every fresh clone — which is exactly the trap this line block warns
# about four comments above.
!scripts/falsifiability.py
!scripts/doc_claims.py
!scripts/baselines/
!scripts/r2_upload_pdfs.py
!scripts/r2_monitor.py
!scripts/legislation_monitor.py
!scripts/update_instrument_provisions.py
!scripts/hub_scrapers/__init__.py
!scripts/hub_scrapers/inner_west.py
!scripts/hub_scrapers/city_of_sydney.py
!scripts/hub_scrapers/woollahra.py
!scripts/hub_scrapers/canterbury_bankstown.py
!scripts/hub_scrapers/waverley.py
!scripts/run_migration_007.py
!scripts/run_migration_008.py
!scripts/populate_waverley_registry.py
!scripts/populate_woollahra_registry.py
!scripts/populate_city_of_sydney_registry.py
!scripts/populate_ku_ring_gai_registry.py
!scripts/survey_dcp.py
!scripts/check_registry.py
# DQ-30 guards — the pre-commit hook invokes the lint; if it is not tracked,
# every commit in a fresh clone fails on a missing file.
!scripts/lint_hardcoded_zone_codes.py
!scripts/lint_hardcoded_confidence.py
!scripts/validate_zone_code_validity.py
!scripts/backfill_invalid_zone_codes.py
!scripts/requirements-monitor.txt
!scripts/requirements-stats.txt
!scripts/secondary_dwelling_stats.py
!scripts/json_to_ts_stats.py
!scripts/dcp_extract_changed.py
!scripts/requirements-extract.txt
!scripts/validate_requirement_data.py
!scripts/validate_pdf_coverage.py
!scripts/verify_dcp_formatting.py
!scripts/smoke_test.sh
!scripts/smoke_parse.py
!scripts/pre-pr-tests.sh
!scripts/validate_toc_join.py
!scripts/install_hooks.sh
!scripts/hooks/
!scripts/README.md
!scripts/templates/
!scripts/templates/sepp_requirement_template.json
!scripts/ingest_spatial_overlays.py
!scripts/build_lot_search_index.py
!scripts/ingest_flood_studies.py
!scripts/validate_flood_e2e.py
!scripts/generate_conveyancing_report.py
!scripts/conveyancing_db.py
!scripts/add_new_chapter.py
!scripts/audit_url_landscape.py
!scripts/confirm_chapter.py
!scripts/pco_client.py
!scripts/dcp_quality_report.py
!scripts/dcp_watchdog.py
!scripts/dcp_commit_approved.py
!scripts/backfill_source_chapter_key.py
!scripts/verify_controls_monitoring.py
!scripts/insert_cb_parking.py
!scripts/insert_penrith_parking.py
!scripts/insert_cumberland_parking.py
!scripts/insert_bayside_parking.py
!scripts/insert_hornsby_parking.py
!scripts/insert_randwick_parking.py
!scripts/insert_georges_river_parking.py
!scripts/insert_sutherland_parking.py
!scripts/validate_parking_extraction.py
!scripts/hub_scrapers/cumberland.py
!scripts/hub_scrapers/penrith.py
!scripts/hub_scrapers/generic.py
!scripts/insert_campbelltown_parking.py
!scripts/insert_liverpool_parking.py
!scripts/insert_waverley_parking.py
!scripts/insert_woollahra_parking.py
!scripts/insert_ku_ring_gai_parking.py
!scripts/insert_blacktown_parking.py
!scripts/insert_parramatta_parking.py
!scripts/insert_inner_west_parking.py
!scripts/insert_northern_beaches_parking.py
!scripts/insert_ku_ring_gai_landscaping.py
!scripts/insert_woollahra_landscaping.py
!scripts/insert_hornsby_landscaping.py
!scripts/insert_cumberland_landscaping.py
!scripts/insert_campbelltown_landscaping.py
!scripts/insert_parramatta_landscaping.py
!scripts/insert_canterbury_bankstown_landscaping.py
!scripts/insert_bayside_landscaping.py
!scripts/insert_liverpool_landscaping.py
!scripts/insert_randwick_landscaping.py
!scripts/scrape_lep_land_use.py
!scripts/batch_lep_scrape.py
!scripts/scrape_cb_lep_pdf.py
!scripts/check_zone_coverage.py
!scripts/manual_verify.py
!scripts/register_orphan_chapters.py
!scripts/insert_georges_river_landscaping.py
!scripts/insert_sutherland_shire_landscaping.py
!scripts/insert_waverley_landscaping.py
!scripts/insert_inner_west_landscaping.py
!scripts/insert_northern_beaches_landscaping.py
!scripts/insert_solar_access_hours.py
!scripts/insert_bicycle_parking.py
!scripts/insert_gap_fill_top_volume.py
!scripts/insert_gap_fill_mid_volume.py
!scripts/qa_gate.py
!scripts/dq_check.py
!scripts/check_dev_server_boots.py
!scripts/check_served_output.py
!scripts/dq_db.py
!scripts/dq_probe_live.py
!scripts/dq_probe_stale_workflow_docs.py
!scripts/dq_probe_dead_sar_constants.py
!scripts/dq_probe_pvlib_claim.py
# DQ-75's probe. It tests the zone-code LINT rather than the corpus, so an
# untracked copy would leave the ledger citing a command CI cannot run — the
# same shape as the trap this block warns about at the top.
!scripts/dq_probe_zone_lint_keyed_map.py
# The DQ-76 repair. It has already run against production, so an untracked copy
# would leave 104 rows of served regulatory text altered with no reviewable
# record of the transform that altered them.
!scripts/repair_tex_residue.py
# DQ-70's "is this change real?" measurement. It is the step that stops a
# re-extraction being run on a chapter that was only recompressed, so it has to
# be re-runnable by whoever works the remaining 262.
!scripts/dq70_measure_text_delta.py
!scripts/qa_report_template.json
!scripts/main_red_alarm_logic_check.js
!scripts/check_test_baselines.py
!scripts/check_dependency_skips.py
!scripts/satellite_freshness_monitor.py
!scripts/run_monitors.py
!scripts/run_alerts_dispatch.py
!scripts/download_narclim_rasters.py
!scripts/numeric_control_review.py
# Weekly maintenance cron jobs (migrated off GitHub Actions, 2026-06)
!scripts/security_scan.py
!scripts/mutation_health.py
!scripts/refresh_stats_job.py
!scripts/requirements-maintenance.txt

# PRP checkpoint working files
prp_checkpoints/*.py

# Tool directories.  These are root-only: an unanchored `schemas/` also
# ignored frontend-nextjs/lib/schemas/, leaving source files invisible to Git.
/poppler/
/schemas/
/test_update/

# Single-word junk files
i
next
of
restart
same
session
taking
supabse

# HTML table extracts
table_*.html

# Misc junk
nsw-compliance-engine-frontend@*
"version"
'version'
.aider*
frontend-nextjs/.wrangler/
*.db-shm
*.db-wal

# Local tool data
.beads/
lancedb_data/

# Archived investigation/fix scripts (kept locally for reference)
# 2026-08-16: "kept locally" is exactly the failure this rule caused — the
# scripts were kept locally and NOWHERE ELSE. scripts/archive/ is now tracked
# so they survive the laptop; the QA gate's content scanners skip that one
# directory (see scripts/qa_gate.py) because it holds retired one-off scripts
# nothing runs, and tests/test_archive_is_not_live_code.py fails if anything
# outside it ever imports from it.
archive/
!scripts/archive/
!scripts/archive/**
parramatta/
frontend-nextjs/check-lep-data.js
ku-ring-gai/
city-of-sydney/
downloads/
reviews/
.env.prod.tmp

# Large GIS data files — ingested into PostGIS, not stored in repo
data/hawkesbury_flood/
data/bushfire/
data/narclim/

# Leigh presentation deck (binary)
leigh-verify.pdf
data/dcps/
data/lep-html/

# Climate risk intelligence — reference PDFs (future product, not compliance engine core)
climate_risk_intel/
temp_dcp_pdfs/

# latent-scope analysis lane — local exports, embeddings, worklists, scopes.
# These are regenerable analysis artifacts, never committed.
data/latent_scope/
latent-scope-data/
.venv-latentscope/

# Sydney Water GSP servicing feature (migration 058) — track the pipeline, not the data blobs
!scripts/ingest_sydney_water_gsp.py
# (docs/servicing/ likewise — the deny-list covers it.)

# (docs/qa/ needed an explicit negation while docs/ was whitelist-only. The
# deny-list above covers it now, so the negation has gone rather than lingering
# as a rule nobody can explain.)

# Schema-contract gate — scripts/ is whitelist-only (line 402), so a new file here
# is silently untracked unless named. Verify with: git ls-files --error-unmatch <path>
!scripts/validate_schema_contract.py
!scripts/schema_contract_baseline.json
!scripts/lint_fabricated_verdicts.py
!scripts/retag_applicability_slug_docids.py
!scripts/validate_controls_provenance.py
!scripts/link_controls_to_provisions.py
!scripts/measure_control_quote_gap.py
!scripts/validate_control_source_values.py
!scripts/control_source_values_baseline.json
!scripts/repair_camden_front_setbacks.py
!scripts/refile_secondary_street_setbacks.py
!scripts/measure_control_type_mismatch.py
!scripts/repair_canada_bay_rear_setback.py
!scripts/repair_missing_primary_controls.py
!scripts/repair_authorized_held_rows.py

# Output-grounding item 3 — per-source "as at" dates (dcp_plan_as_at)
!scripts/fetch_dcp_as_at_dates.py
!scripts/extract_dcp_stated_dates.py
!scripts/check_dcp_as_at_coverage.py
!scripts/repair_dq41_null_label_derived_dates.py
# DQ-62 — the three commencement dates that were amendment/consolidation stamps
!scripts/repair_dq62_commencement_dates.py
# Has anybody checked the live data lately (guards the data-watch schedule)
!scripts/check_data_watch_freshness.py
# Scores the served ANSWER — provenance, currency, applicability
!scripts/check_served_answer_quality.py

# Output-grounding item 4 — execution manifests + units/CRS checks
!scripts/check_satellite_manifests.py

# Output-grounding item 5 — TS direct-reader consolidation
!scripts/check_ts_dcp_readers.py
# Shadow calibration damage census (read-only; repair is a separate pass).
!scripts/measure_shadow_calibration_damage.py
# The authorized repair for the 19 impossible-reach reports — regenerates them
# through the pipeline's own functions, with backup/predict/guard/verify/
# idempotence/rollback. Committed so the production write can be audited and
# rolled back from the repo rather than from memory.
!scripts/repair_shadow_reports.py

# Structure-detection ground truth. The sampler must be committed so the sample
# can be redrawn and audited by anyone; the recall script must be committed
# because it carries the pass mark, and a threshold that lives only on one
# machine is not a pre-commitment.
!scripts/sample_structure_label_set.py
!scripts/measure_structure_detection_recall.py

# Per-branch push-attempt counter for push-retry-needs-full-output.py.
# Session state, not repo state.
.claude/.push-attempts.json
